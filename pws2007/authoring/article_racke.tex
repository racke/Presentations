%% -*- mode: latex; -*-

\section{SpamAssassin, Antivirus und Email via Perl}

\subsection*{Autor}
Stefan Hornburg (Racke) \verb/<racke@linuxia.de>/

\subsection*{Bio Stefan Hornburg}
Stefan Hornburg arbeitet seit 1998 als Open Source Consultant mit den
Schwerpunkten Linux, Perl und Interchange. 

%% with Linux, Perl and Interchange (the former MiniVend) as main
%% fields of interest. System administration belongs also to
%% his daily business. His customers are spread over the world,
%% most of them in the USA, but also in Austria, Switzerland, Germany
%% and Canada.

%He became one of the main developers of Interchange relatively
%early in his consultant career and serves now as captain of
%the ICDEVGROUP, which took over the responsibility and maintenance
%of this eCommerce software from RedHat.
%Additionally, he maintains Debian packages for the Courier, PureFTPd,
%%Interchange and Sympa servers.
 
\subsection*{Abstract}
Die Bedeutung von Email für die Kommunikation hat einen hohen
Stellenwert erreicht und ist oftmals nicht aus dem täglichen
Leben und aus der Arbeitswelt wegzudenken. Attacken auf dieses
wichtige Instrument mittlels SPAM, Viren und Phishing nehmen
kontinuierlich zu. Nützliche Emails drohen in einer Flut von unerwünschter
und potentiell schädlichen Post unterzugehen, wenn Gegenmaßnahmen
nicht oder nur unzureichend greifen.

Für Perl sind eine Reihe von Modulen vorhanden, die die Erstellung
von Programmen zum Zugriff auf Email und die Bewertung der Email
erleichtern:

\begin{itemize}
\item Net::SMTP (Emailversand)
\item Mail::IMAPClient (Zugriff auf Email mit IMAP)
\item Mail::SpamAssassin (SpamAssassin)
\item ClamAV::Client (Clam Antivirus)
\end{itemize}
	
Nach einer kurzen Einführung in die Benutzung dieser Module beschäftigt
sich der Hauptteil des Vortrags mit der Beschreibung von Anwendungen
zur Verbesserung der Abwehr von SPAM, Viren und Phishing.

Zur Demonstration werden Emails von einem IMAP-Server heruntergeladen 
und von ClamAV auf Viren und Phishing geprüft und anschließend durch
SpamAssassin bewertet. Anschließend wird gezeigt, wie diese Emails vom
Server gelöscht oder in bestimmte Verzeichnisse auf dem IMAP-Server verschoben
werden können. Die Behandlung von Fehlerkennungen (\WSIndex{False Positives})
wird ebenso erläutert.

Im Anschluß wird die Erstellung von Statistiken über das Aufkommen
von Viren und SPAM besprochen und wie man daraus Schlüsse zur Optimierung
zur Abwehr treffen kann.

\subsection{Mailversand mit \WSIndex{Net::SMTP}}

Eine SMTP-Verbindung wird durch die Erzeugung eines
\verb/Net::SMTP/-Objektes mit der Angabe des Mailservers
aufgebaut.

\begin{verbatim}
use Net::SMTP;

my $smtp = new Net::SMTP ($server);
\end{verbatim}

Über diese Verbindung können nun eine oder mehrere Emails abgesetzt
werden.

\begin{verbatim}
$smtp->mail($from});
$smtp->to($to});

$smtp->data();
$smtp->datasend(<<EOF);
To: $to}
Subject: Hello world

Welcome to Net::SMTP.
EOF

$smtp->dataend();
\end{verbatim}

Danach beendet man die Verbindung.

\begin{verbatim}
$smtp->quit();
\end{verbatim}

\subsection{Mailempfang mit \WSIndex{Mail::IMAPClient}}

Eine IMAP-Verbindung kann einfach durch die Erzeugung eines
\verb/Mail::IMAPClient/-Objektes
mit den geeigneten Parametern hergestellt werden. Dafür müssen
zumindestens der Servername (Server), Benutzername (User) und Passwort
(Password) übergeben werden. Dann erfolgt der Verbindungsaufbau und die
Anmeldung automatisch.

\begin{verbatim}
use Mail::IMAPClient;

my %params = (Server => $server,
              User => $login,
              Password => $password);

my $client = new Mail::IMAPClient (%params);
\end{verbatim}

Dies ist nicht möglich, wenn man eine SSL-geschützte Verbindung aufbauen
möchte. In diesem Fall erzeugt man die Verbindung mit dem
\verb/IO::Socket::SSL/-Modul: 

\begin{verbatim}
use IO::Socket::SSL;
use Mail::IMAPClient;

my $conn;

unless ($conn = new IO::Socket::SSL->new ("${server}:imaps")) {
    die "$0: imaps connection to $server failed: $!\n";
}

$imap = new Mail::IMAPClient (Socket => $conn,
							  User => $user,
							  Password => $password);
\end{verbatim}

Unbekannte Methoden werden direkt in IMAP-Befehle umgesetzt, was durchaus
zur Verwirrung des Programmierers führen kann. Andererseits kann es genutzt
werden, um proprietäre Erweiterungen des IMAP-Befehlssatz anzusprechen.

\subsubsection{Emails suchen}

Der \verb/SEARCH/-Befehl ist das mächtigste Kommando und dient zur
serverseitigen Suche. Die Suche erstreckt sich jedoch nur auf einen
zuvor ausgewählten Ordner.

\begin{verbatim}
$imap->select($folder);

@msgs = $imap->search('SUBJECT', $imap->Quote('Rechnung'));

if ($@) {
    die "$0: IMAP search failed: " . $imap->LastError();
}
\end{verbatim}

\subsubsection{Emails abrufen}

Die in einer Verzeichnisstruktur gespeicherten Emails werden durch
Auswahl eines Ordners und einer Suche in diesem Ordner in eine
Liste von Messagenummern überführt.

Mit diesen Nummern ist eine Vielzahl von Operationen möglich,
wie z.B. die komplette Email in einen String zu schreiben:

\begin{verbatim}
$imap->select($folder);

@msgs = $imap->search('ALL');

if ($@) {
    die "$0: IMAP search failed: " . $imap->LastError();
}

for my $msgno (@msgs) {
    $message = $imap->message_string($msgno);
}

\end{verbatim}

\subsubsection{Emails löschen oder verschieben}

Das IMAP-Protokoll bietet keine Möglichkeit Emails unmittelbar
zu löschen. Die Emails werden zunächst als gelöscht markiert und beim
nächsten Aufräumvorgang physikalisch gelöscht.

Die \verb/delete_message/-Methode führt den folgenden IMAP-Befehl aus:
\begin{verbatim}100 UID store 3,4,5,6,7 +FLAGS.SILENT (\Deleted)\end{verbatim}. 
Die Emails sind weiterhin sichtbar. Durch das Schließen des
IMAP-Ordners mit \verb/close/ (implizit) oder dem expliziten \verb/expunge/
werden die Emails dann auch physikalisch gelöscht.

Das Verschieben einer Email wird wie folgt implementiert:

\begin{itemize}
\item Emails aus Originalordner in Zielordner kopieren
\item Email im Originalordner als gelöscht markieren
\item Originalordner aufräumen
\end{itemize}

Die IMAP-Befehle dazu sehen wie folgt aus:

\begin{verbatim}
100 UID COPY 21,24,43,46,65 INBOX.Junk
101 UID store 21,24,43,46,65 +FLAGS.SILENT (\Deleted)
102 expunge
\end{verbatim}

\subsection{Virenscanner mit \WSIndex{ClamAV::Client}}
Das Modul \verb/ClamAV::Client/ bietet eine Schnittstelle zum ClamAV
Virenscanner. Beim Anlegen des Objektes wird eine Verbindung zum 
\verb/clamd/-Daemon aufgebaut.

\begin{verbatim}
use ClamAV::Client;

my $scanner;

$scanner = new ClamAV::Client;

unless (defined $scanner && $scanner->ping()) {
    die "$0: connection to ClamAV daemon failed\n";
}
\end{verbatim}

Mit den Parametern \verb/socket_name/, \verb/socket_host/,
\verb/socket_port/ kann die Art der Verbindung (TCP oder Unixsocket)
und die Verbindungsparameter definiert werden, falls \verb/clamd/ nicht
am Default-Socket \verb%/var/run/clamav/clamd.ctl% lauscht.

Mit Hilfe der \verb/version/-Methode kann man bestimmen, ob die Virusengine
und die Virusdatenbank von ClamAV auf dem neuesten Stand ist:

\begin{verbatim}
my ($engine, $dbrelease, $dbdate) = split('/', $scanner->version());

print <<EOF;
Engine: $engine
Database release (daily.cvd): $dbrelease
Database date: $dbdate
\end{verbatim}

Das Resultat sieht dann so aus:

\begin{verbatim}
Engine: ClamAV 0.87.1
Database release(daily.cvd): 2466
Database date: Fri Jan 19 00:49:11 2007
\end{verbatim}

\subsection{Spamfilter mit \WSIndex{Mail::SpamAssassin}}
Im Gegensatz zu den anderen Modulen wird beim Anlegen eines
\verb/Mail::SpamAssassin/-Objekts keine Verbindung zum Spamasssassin-Dämon
\verb/spamd/ aufgebaut. 

\begin{verbatim}
use Mail::SpamAssassin;

$sa = new Mail::SpamAssassin;
\end{verbatim}

Der erste Schritt beim Auswerten einer Email mit \verb/Mail::SpamAssassin/
ist das Erzeugen eines \verb/Mail::SpamAssassin::Message/-Objektes.
Dazu werden nur die Header der Email ausgewertet, wenn es nicht explizit
anders angegeben wird.

\begin{verbatim}
$message = $imap->message_string($msgno);
$samsg = $sa->parse($message);
\end{verbatim}

Die \verb/check/-Methode überprüft die Email und liefert ein 
\verb/Mail::SpamAssassin::PerMsgStatus/-Objekt zurück. 

\begin{verbatim}
$status = $sa->check($samsg);
$num = $status->get_score();

if ($status->is_spam()) {
    print "SPAM ($num)\n";
} else {
    print "HAM ($num)\n";
}
\end{verbatim}

\subsection{Scenarios}
%\subsubsection{Bayes}
%Update Bayes from FN/FP.
%Die Emaildateien koennten auch direkt aus dem Dateisystem gelesen werden,
%aber bei Zugriff mit IMAP brauchen wir keine Annahmen ueber die 
%Verzeichnisstruktur zu machen bzw. koennen Mailserver und SA-Server auf
%verschiedenen Rechnern sein.

%Problem: verbraucht viel RAM



\subsubsection{Virus}
Mit einer lokalen Installation von ClamAV können Postfächer auf anderen
Servern auf Malware untersucht werden. Zwar wird jeder seriöse Emailprovider
eingehende Emails auf Viren prüfen, aber es gibt doch gute Gründe einen
eigenen Check zu fahren:

\begin{itemize}
\item Viren können im Postfach landen bevor diese in die Virusdatenbank des
  verwendeten Virenscanners aufgenommen wurde
\item jeder Virenscanner hat eigene Stärken und Schwächen bei der Erkennung
\item ClamAV erkennt Malware aller Art, darunter auch \WSIndex{Phishing} und
  \WSIndex{Trojaner}
\item Emails mit Viren können durch IMAP-Operation ins Postfach gelangen
\end{itemize}

Emails mit Malware spiegelt dem Empfänger vor, von seriösen Unternehmen wie
Banken, Telekommunikationsunternehmen oder sogar der GEZ versandt worden zu
sein. Im folgenden Beispiel betrachten wir nur Emails mit dem typischen
Betreff Rechnung.

\begin{verbatim}
$imap->select('INBOX');

@msgs = $imap->search('SUBJECT', $imap->Quote('Rechnung'));

for my $msgno (@msgs) {
    $message = $imap->message_string($msgno);

    $result = $scanner->scan_scalar(\$message);

    if ($result) {
        print "INFECTED: $result\n";
        push(@badmsgs, $msgno);
    } else {
        print "OK\n";
    }
}
\end{verbatim}

Die als schädlich erkannten Emails werden anschließend gelöscht:

\begin{verbatim}
$imap->delete_message(@badmsgs);
$imap->expunge();
\end{verbatim}

%\subsubsection{SpamAssassin}
%Scan mails on remote server and move them, idea: better SPAM fighting
%from local machine, especially on new waves.

\subsection{Statistiken}

\begin{thebibliography}{99}
\bibitem{racke:imaprfclist} IMAP Documents: RFCs and Drafts
\texttt{http://www.imap.org/papers/biblio.html}
\end{thebibliography}

